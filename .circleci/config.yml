
version: 2.1
jobs:
  build:
    docker:
      - image: cypress/base:10
    steps:
       - checkout
       - run:
          name: Install Cypress
          command: npm install cypress
       - run:
          name: Run cypress
          command: npm run cypress
